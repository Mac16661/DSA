#include<bits/stdc++.h>
using namespace std;

// Graph traversal pattern(also travers disjoint graphs)
void connectedTraversal(int E, int V, vector<vector<int>> edges) {
    vector<int> visited(0, E);

    
}

int main() {

    return 0;
}